# OS
.DS_Store
Thumbs.db

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Shell / process artifacts
*.log
*.pid

# Secrets (defense-in-depth — credentials never belong in this repo)
.env
.env.*
!.env.local.example
*.key
*.pem
secrets/

# Deploy backups (binary archives, never version-control)
_backups/
