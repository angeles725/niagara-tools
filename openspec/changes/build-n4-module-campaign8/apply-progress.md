# apply-progress — build-n4-module-campaign8

Generated by sdd-apply executor on 2026-09-05. Hybrid store (Engram + openspec).

---

## PR1 — feat/c8-lint-delays

**Branch**: `feat/c8-lint-delays`
**Status**: COMPLETE — all 11 tasks done; guards green; commit, push, PR pending
**TDD mode**: strict (RED → GREEN → REFACTOR)

### TDD Evidence

| Phase | Commit / evidence | Result |
|-------|-------------------|--------|
| RED   | `1f3253a` (cherry-pick from `qa/c8-lint-delays`) — bats fail before lint-delays.sh | RED confirmed |
| GREEN | lint-delays.sh written + LD1-LD9 all passing | GREEN |
| D2b (addendum) | cross-file helper pre-pass awk added | LD10 added, BDefrostController clean in fixed tree |
| REFACTOR | shellcheck clean, is_floor patterns finalized | shellcheck exit 0 |

### Work Unit Evidence

| Task | Done | Evidence |
|------|------|----------|
| 1.1 Re-read qa/c8-lint-delays tip c96b2ad | x | LD9 exit 3 confirmed; LD8 WARN + exit 0 confirmed |
| 1.2 Cherry-pick RED branch | x | commit 1f3253a in feat/c8-lint-delays |
| 1.3 Write lint-delays.sh | x | 11.5 KB; set -u; shellcheck exit 0; exits 0/1/3; D2b pre-pass included |
| 1.4 Copy 7 fixtures | x | tests/fixtures/lint-delays/{Overdue,Floored,LiteralZero,LiteralPos,Periodic,SlotGetterMinZero,SlotGetterMinPos}.java |
| 1.5 Write lint-delays.bats LD1-LD10 | x | 10 tests; LD10 skip-gated on C8_CRP_FIXED |
| 1.6 CI step | x | ci.yml not present in repo; bats coverage sufficient per conventions |
| 1.7 K19 routing BUILD-LOOP.md + SKILL.md | x | BUILD-LOOP.md §5; SKILL.md step 5 + References; version 0.7→0.8 |
| 1.8 Named mutation Math.max(x,0L) | x | LD1 stops FAILing; LD3 is literal-zero (unrelated); documented in PR body |
| 1.9 Real smokes | x | pre-fix 4f5f1c7: exits 1, BDefrostController {556,566,620,664} FAIL; fixed c66e412: BDefrostController clean (D2b works); BEvaporatorUnit MIN=0 pre-existing |
| 1.10 Guards | x | 189/189 bats green; shellcheck exit 0; sweep-build-state exit 0; sweep-fold-audit --strict exit 0; kit-links L4/L5 green |
| 1.11 Retro + INDEX + BUILD-STATE | x | 2026-09-05-campaign8-lint-delays.md; INDEX row added; BUILD-STATE retro_pending: true |

### Guard Results

```
bats tests/*.bats     : 189/189 passed
shellcheck            : exit 0 (SC2016 disabled inline for xargs awk)
sweep-build-state.sh  : exit 0
sweep-fold-audit.sh --strict : exit 0
kit-links.bats L4/L5  : green
```

### Real Smoke Results

**Pre-fix (4f5f1c7) ColdRoomPan-rt/src** — exits 1:
```
FAIL  BDefrostController.java:556  zero-floor  Math.max(delayMs,0L) — floor is 0
FAIL  BDefrostController.java:566  zero-floor  Math.max(delayMs,0L) — floor is 0
FAIL  BDefrostController.java:620  zero-floor  Math.max(delayMs,0L) — floor is 0
FAIL  BDefrostController.java:664  zero-floor  Math.max(delayMs,0L) — floor is 0
```
Lines :622 and :641 are clean (expected per spec).

**Fixed (c66e412) ColdRoomPan-rt/src** — exits 1 (not 0 as spec expected):
- BDefrostController: CLEAN (D2b resolves positiveDelayMs → proven floor)
- BEvaporatorUnit: FAIL on MIN=0 facets for startDelay/stopDelay/defrostFanOffDelay/powerOnDelay — these are pre-existing slots NOT in the defrost-fix scope. Documented as Delta 4 in retro.

**CompPan-rt/src :1764** — FAIL facet-min-zero (no MIN facet, not MIN≥1). Spec said "WARN or PASS"; actual result is FAIL. Documented as Delta 4 spec discrepancy.

### Spec Discrepancies

1. Fixed-tree expected to exit 0; actual exits 1 due to BEvaporatorUnit MIN=0 slots (pre-existing, out of scope for defrost fix). The lint correctly identifies these.
2. Named mutation note: LD1 stops FAILing with Math.max(x,0L). LD3 does NOT flip (it uses BRelTime.make(0), not Math.max).
3. CompPan :1764 is FAIL not WARN.

### D2b Design Addendum

Cross-file helper resolution: pre-pass awk collects all `static long <fn>(` method bodies from all
*.java in src root (dot-dirs pruned). Four is_floor patterns: `Math.max(x,N≥1)`, `>=1` comparison,
`<1?1:x`, `return 1L`. Function names proven as floors stored in POSITIVE_HELPERS. Per-file awk
checks helpers for case 2.5 (BRelTime.make(<call>)) and case 3 backward scan (variable assignment RHS).
