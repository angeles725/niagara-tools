# Exploration — build-n4-module-kit-v0.2

**Date**: 2026-09-01
**Phase**: explore
**Status**: complete
**Engram**: topic_key `sdd/build-n4-module-kit-v0.2/explore`
**OpenSpec target**: `openspec/changes/build-n4-module-kit-v0.2/explore.md`

---

## A. Current State Map

### Launcher — `~/.claude/skills/build-n4-module/SKILL.md`

57 lines, v0.1, Apache-2.0. A thin launcher that resolves the kit path and states hard rules. Contains three hardcoded absolute paths (all confirmed to resolve on this machine per QA audit):
- `python3 /home/cristian/niagara-research/tools/corpus-nav.py`
- `python3 /home/cristian/niagara-research/tools/dashboard-preview.py` (in BUILD-LOOP.md step 3 too)
- Kit default: `/home/cristian/modulos_niagara_n4/niagara-tools/build-n4-module-kit`

One inconsistency: the launcher says "Build = `$KIT/toolbelt/build.sh`", but BUILD-LOOP.md step 4 and build-verify.md say the primary build is `ng-deploy.sh --strict-slotomatic` with build.sh as **fallback**. These contradict each other.

### Kit — `/home/cristian/modulos_niagara_n4/niagara-tools/build-n4-module-kit/`

| File | Lines (est.) | Maturity | Notes |
|---|---|---|---|
| README.md | 21 | stable | layout + resolve instructions |
| METHODOLOGY.md | ~30 | mature | common verify checklist for all types |
| BUILD-LOOP.md | 35 | mature | operational cycle; hardcodes dashboard-preview.py path |
| build-verify.md | 44 | mature | Java-8 + slotomatic recipe; missing deploy-target/signing section |
| SOURCES.md | 28 | mature | ColdRoomPan exemplar cites `/mnt/c/...` Windows path; `/home/cristian/modulos_niagara_n4/Cliente/Leon-Guanjuato/Paccadia/ColdRoomPan/` ALSO EXISTS and is the preferable WSL path |
| types/dashboard.md | ~55 | MATURE | proven end-to-end; lacks extending-existing-dashboard, chart, plano, and UX-overflow sections |
| types/logic.md | ~12 | SEED | stubs only; cites `checklist-common.md` (does not exist — real name: METHODOLOGY.md) and `type-dashboard.md` (does not exist — real name: types/dashboard.md) |
| types/wb-widgets.md | ~10 | SEED | stubs only; same dangling relative refs as logic.md |
| toolbelt/build.sh | 35 | has bugs | see section C |
| retros/ (3 files) | ~200 total | untracked | 3 retro files exist on disk but are **untracked in niagara-tools git** (never committed); .gitkeep committed |

### openspec/ directory (niagara-tools)

Contains one prior change `niagara-tools-slotomatic-integration` (5 files: explore, proposal, spec, design, tasks) already released as v0.3.0. Leave these untouched. The `build-n4-module-kit-v0.2/` subdirectory does not exist yet.

### niagara-tools repo state

- VERSION: 0.3.0
- CONTRIBUTING.md §2: strict TDD for scripts/ (red→green→refactor, bats + shellcheck gate)
- tests/ng-deploy.bats: 26 cases, covers the existing ng-deploy.sh surface
- `scripts/ng-deploy.sh`: 497 lines, exec bit lost in working tree (mode 100755→100644, zero content diff) — real defect, must be fixed before commit
- `bats`: NOT installed (brew install bats-core available); `shellcheck`: installed (linuxbrew)
- ng-deploy.sh verify step: checks ONLY `<type` count in module.xml, NOT bytecode major 52, NOT NIAGARA4.SF presence, NOT baja vendorVersion
- ng-deploy.sh slotomatic: runs only `:MODULE-rt:slotomatic`; DashboardPan-ux has a `@NiagaraType` servlet, so -ux annotation edits are silently never regenerated by ng-deploy (build.sh runs `:ux:slotomatic` correctly via the profile-loop)

---

## B. Consolidated Delta Inventory

Sources: 3 kit retros + bitácora mirror-niagara-home.sh + QA addendum. Investigador2 pre-deduped these. Items marked "already covered" in retro 3 are excluded. Items from niagara-research retros that target the research-sdd kit (not build-n4-module kit) are excluded.

**Total net-new fold-in items: 28** (A: 4, B: 8, C: 13, D: 2, E: 1) plus 3 system-level inconsistencies (F1–F3) and QA-sourced additions (G-QA).

### Group A — `types/logic.md` (from rt-hardening retro)

| ID | Lesson | Evidence | Status |
|---|---|---|---|
| A1 | Restart-persistent internal timer = `HIDDEN BAbsTime` slot; NOT `TRANSIENT`; compute elapsed on `atSteadyState`; re-arm `max(interval-elapsed,0)`; `isNull()` → full interval | BColdRoom/BDefrostController, verified API: Clock.java BAbsTime.java | NEW |
| A2 | Plain non-BObject helper class (`CrLog`) compiles and bundles; use one for a shared `java.util.logging.Logger` so `logError()` swallows+logs on engine thread | CrLog.class shipped in ColdRoomPan jar | NEW |
| A3 | Safety fail-mode as `SUMMARY|OPERATOR` config slot, default = current behavior; never a baked-in constant | plant-safety posture; hardening must not silently change control on live plant | NEW |
| A4 | Adding slots to an already-generated rt component regenerates cleanly; hand-write annotation + matching AUTO region stub, then run slotomatic — slotomatic is authoritative | BColdRoom type hash regen 1765798213 | NEW — confirms METHODOLOGY item, adds concrete pattern |

Also target `types/logic.md`: fix dangling relative refs (`checklist-common.md` → `METHODOLOGY.md`; `type-dashboard.md` → `types/dashboard.md`). Same fix in `types/wb-widgets.md`. (QA finding, not from retros.)

### Group B — `build-verify.md` (from rt-hardening + 5rooms retros)

| ID | Lesson | Evidence | Status |
|---|---|---|---|
| B1 | Build against `niagara_home` of the LOWEST target station version; manifest stamps `baja vendorVersion`; a 4.14 station REJECTS a jar built against 4.15 | 4.15→4.14 rebuild; `unzip -p jar META-INF/module.xml | grep baja` | NEW |
| B2 | Pinned `gradlePluginVersion` in `settings.gradle.kts` MUST exist in `<niagara_home>/etc/m2`; make it overridable via `-PniagaraPluginVersion=<v>` | Honeywell 4.14 ships 7.6.17; PowerB 4.15 ships 7.6.22 | NEW |
| B3 | `gradle.properties` `niagara_home` can LIE; true build target = intersection(pinned plugin present in etc/m2, station accepts stamped baja); verify plugin version before trusting gradle.properties | 4.13.2 vs 4.14 mismatch incident | NEW |
| B4 | Windows `niagara_home` path (`C:\...`) breaks the m2 repo under WSL; always override with `/mnt/c/...` mount | "plugin not found" in WSL with Windows path | NEW |
| B5 | Running station locks `<niagara_home>/modules/<module>.jar`; build against a READ-ONLY MIRROR (symlinks to all modules except the one being built, writable `modules/`); recipe: mirror-niagara-home.sh (see E1) | `Unable to delete file … modules/ColdRoomPan-rt.jar` | NEW |
| B6 | Check the deploy target's signing policy BEFORE assuming Workbench re-sign is needed; OptimizerSupervisor accepts gradle DEV cert; JACE field controller enforces project CA (angelessigner/luissigner) | chihuahua never re-signs; ColdRoomPan JACE-bound must re-sign | NEW |
| B7 | Workbench `JarFileSigner` "invalid entry compressed size (expected N but got M)" = DEFLATER MISMATCH between the gradle JDK (WSL OpenJDK 8) and the Workbench JDK (Windows Zulu 8); **CORRECTION of rt-hardening 2026-08-31 hypothesis**: NOT transient build state, NOT fixed by `clean+rebuild`; local jarsigner is a **FALSE NEGATIVE**; reliable fix = STORED repackage (`unzip -q`→`zip -0 -X -D`); field-confirmed `[CERT-live 2026-09-01]` | DashboardPan STORED jars signed in Workbench with NO ZipException | NEW; supersedes the rt-hardening hypothesis |
| B8 | `niagaraTest` does NOT run in WSL (needs `bin/test.exe` + dev license); concrete alternative: extract decision into a zero-Baja pure class, compile+run with plain `javac -source 8 -target 8` + JUnit4 JARs; `srcTest/` tests are dead weight in WSL | ColdRoomControl.decideCall + ColdRoomControlTest; METHODOLOGY already says "unit-test the pure-Java model" — this adds the concrete how + why | NEW (concrete recipe) |

Also add to `build-verify.md`: a "Deploy target & signing" checklist synthesizing B1–B7.

### Group C — `types/dashboard.md` (from 5rooms + hmi-touch-ux retros)

| ID | Lesson | Evidence | Status |
|---|---|---|---|
| C1 | When a room's field count exceeds what fits in 2 columns at ≥44px targets, partition fields into named sub-tab GROUPS (`groups:[{name,fields}]` + `.cfg-tabs` bar); one shared `items[]` + one save | Cuarto 3: 15 fields; scroll rejected, 3-col rejected; sub-tabs accepted by operator | NEW |
| C2 | SVG chart: size `viewBox` to the measured box px each render (`chart.getBoundingClientRect()`); kill fixed art-board viewBox with `preserveAspectRatio="none"` | `renderChart` box-measure fix on WEB-HMI10 | NEW |
| C3 | Touch HMI chart read-out = pointer-event crosshair (`pointerdown/move` + `touch-action:none`); label points by operator name (`equipo`), never raw tag | WEB-HMI10 (10.1" capacitive) touch tooltip | NEW |
| C4 | Plano overlay alignment = four values as one atomic unit: (1) `#plano` image, (2) `IMG_W/IMG_H`, (3) zones `viewBox`, (4) `.frame aspect-ratio`; changing the plano = update all four together; call `ajustarCaja()` on every path that reveals the plano | 3→5 rooms: moved image+vars+viewBox but left `.frame:1247/771` stale → polygons offset | NEW |
| C5 | "Missing UI element" triage order: (a) confirm renders in current `src/rc` on `/hmi`, (b) `unzip -p` the deployable jar and grep for it, (c) THEN suspect stale deployed `-ux`; do not touch code until live-vs-source gap is ruled out | `terminateOnResistanceTemp`/`coolOnSensorFault` stale-ux incident | NEW |
| C6 | Data-driven SPA scales room-count by swapping data arrays (`CUARTOS`, `SENSORS`, `FANS`, plano vars), NOT touching render code; verify with `node --check` on extracted `<script>` blocks after | 3→5 rooms: zero changes to render functions | NEW |
| C7 | Module is the SKELETON (integration: kiosk CSS, `/api/equipment` wiring, status classes, alarms, setpoint save); operator's standalone HTML is the DATA source; correct merge = module as base, splice calibrated data blocks in, remap ords | Rebuilding from standalone HTML loses integration layer; confirmed the hard way | NEW |
| C8 | Facade room count is also data-driven: one `@NiagaraProperty(name="CuartoN", type="BRoomPanel")` per room + one entry in reader array; `BRoomPanel` carries all point slots for free | DashboardReader.ROOMS array drives whole `/api/equipment` walk | NEW |
| C9 | Multi-series chart: assign an explicit distinct color per series; mark shown series visually bold; do not rely on inherited `currentColor` or hover-only emphasis classes | `SERIE_COL` palette; `.on` class applied to visible series on filter | NEW |
| C10 | Setpoint/limit min·max·step·decimals for a dashboard live in the SPA field descriptor array (`SP_COMMON`/`SP_DEFROST`); check facade+control facets too before concluding a range is set in Java | `min:-40` clamp was in SPA field descriptor, not in `@NiagaraProperty` facet | NEW |
| C11 | Extract `buildField(room, f, items)` as the row-builder from the start; grouping, reordering, and conditional fields become cheap | Sub-tab grouping required building same row into N grids | NEW |
| C12 | Zone-label collision at centroid of adjacent/concave rooms → add optional `lbl:[x,y]` (in %) to the room object; data-only, reversible, no geometry change | Cuarto 2 vs Cuarto 5 (L-shaped) label overlap | NEW |
| C13 | `#plano` src can be an inline base64 (not a file path); read the REAL src before trusting an on-disk file — the on-disk file may be orphaned | Grabbed wrong 1247×771 file; real image was 1248×891 base64 | NEW |

### Group D — `METHODOLOGY.md` (from hmi-touch-ux retro)

| ID | Lesson | Evidence | Status |
|---|---|---|---|
| D1 | Asset-laden single-file artifact (dashboard SPA with embedded base64): navigate with `sed -n 'A,Bp' \| cut -c1-160` for known ranges; search with line-length filter (`awk 'length<300'` or python enumerate+len); re-verify edits with `node --check` on extracted `<script>` blocks | DashboardPan `index.html`: base64 data URIs on single multi-MB lines defeated Read tool | NEW |
| D2 | When a prior version renders correctly, diff its plano config against the broken one — same technique, hunt the one inconsistent value (reference: `DashboardPan-Leon` 3-room config isolated the stray `.frame` aspect-ratio) | `.frame:1247/771` isolated by diffing vs the 3-room reference | NEW |

### Group E — Toolbelt additions

| ID | Action | Source | Status |
|---|---|---|---|
| E1 | Promote `mirror-niagara-home.sh` from bitácora to `toolbelt/mirror-niagara-home.sh`; reference from `build-verify.md` B5 recipe | `Cliente/Leon-Guanjuato/bitacora/mirror-niagara-home.sh` — 36 lines, complete, well-documented, field-proven | NEW (promote) |

### Group F — System-level inconsistencies (no retro source; discovered in audit)

| ID | Issue | Fix target |
|---|---|---|
| F1 | Launcher SKILL.md says build = `$KIT/toolbelt/build.sh`; kit (BUILD-LOOP.md + build-verify.md) says primary = `ng-deploy.sh --strict-slotomatic`, build.sh = fallback | Update SKILL.md Hard Rules to match kit's primary/fallback ordering |
| F2 | Kit retros lack a machine-readable `<!-- review-status: pending -->` marker; niagara-research retros have it for `sweep-retros.sh` | Add `<!-- review-status: propose-never-apply -->` (or `folded v0.2`) to each kit retro after fold-in |
| F3 | `types/wb-widgets.md` stays SEED — no wb build happened; no new content to fold | No change; wb stays SEED; fix only dangling relative refs |

### Group G-QA — QA addendum findings (no retro source)

New scripts and tests proposed by QA audit against real DashboardPan jars:

**Automatable verify-module checks** (all confirmed against real jars):
- All-classes bytecode major 52 (every `.class` in jar, not just first)
- `NIAGARA4.SF` presence in each jar
- STORED check: `unzip -v jar | grep -c 'Defl:'` == 0 (opt-in for JACE targets)
- baja `vendorVersion` in `META-INF/module.xml` vs declared target version
- Packaged types in `module.xml` `<type>` entries vs `.class` presence + count vs `src/module-include.xml`
- Raw-double facet grep: `BFacets.make(BFacets.(MIN|MAX), -?[0-9]` must be empty
- Pinned gradle plugin version exists in `<niagara_home>/etc/m2` (preflight)

**NOT automatable** (need human/agent judgment): slot flag/facet semantics, lexicon, domain correctness, HMI fit, touch targets, RBAC.

**QA-proposed tasks**:
- **T1** `toolbelt/verify-module.sh` — standalone script running the 7 automatable checks
- **T2** `tests/verify-module.bats` — bats tests for T1 with generated fixture jars (forged major byte, missing SF, module.xml naming missing class)
- **T3** `tests/build-n4-kit-links.bats` — every relative ref in kit `*.md` resolves (catches dangling refs in logic.md + wb-widgets.md)
- **T4** `build.sh` fixes + bats test that a stub `-wb` dir is skipped

**build.sh bugs** (QA-confirmed):
- Profile detection by directory existence: `DashboardPan/DashboardPan-wb/` is an empty stub subproject → build.sh adds wb tasks to every DashboardPan build
- Bytecode major-version check inspects only the FIRST `.class` per jar; should check all
- Bare `${1:?}` error on no args — no usage message printed
- Spanish error messages (repo convention: English artifacts)
- Stale usage example path (`/home/cristian/niagara-modules/DashboardPan-Leon`)

**ng-deploy.sh issues** (QA-confirmed, no fix needed in this change — inform the verify-module.sh design):
- Verify step checks only `<type` count; does NOT enforce bytecode/NIAGARA4.SF/baja vendorVersion gate
- Slotomatic runs only `:MODULE-rt`; `-ux` annotation edits never regenerated (build.sh does the right thing; ng-deploy.sh design choice, not a bug to fix in this change)

**Exec bit defect** (pre-established):
- `scripts/ng-deploy.sh` lost exec bit (mode 100755→100644, zero content diff); must be restored via `git update-index --chmod=+x scripts/ng-deploy.sh`

---

## C. Tooling & Path Findings

| Finding | Severity | Action |
|---|---|---|
| All 14 absolute paths in kit + launcher resolve on this machine | — | No change needed |
| ColdRoomPan exemplar: `/mnt/c/...` path in SOURCES.md; `/home/cristian/modulos_niagara_n4/Cliente/Leon-Guanjuato/Paccadia/ColdRoomPan/` ALSO EXISTS | LOW | Update SOURCES.md to cite the `/home/cristian/...` path as primary (WSL-native); note `/mnt/c/...` as Windows-only fallback |
| Dangling relative refs: `types/logic.md` + `types/wb-widgets.md` cite `checklist-common.md` (→ METHODOLOGY.md) and `type-dashboard.md` (→ types/dashboard.md) | MEDIUM | Fix in-file; T3 (kit-links.bats) will catch regressions |
| `mirror-niagara-home.sh` only in bitácora | HIGH | Promote to toolbelt/ (E1) |
| `deploy.sh` exists at `Cliente/Leon-Guanjuato/Dashboard/deploy.sh` and `chihuahua/deploy.sh` but not in kit | LOW | Reference from build-verify.md; promotion out of scope for v0.2 |
| `ng-deploy.sh` exec bit lost | HIGH | Fix via `git update-index --chmod=+x`; bats anti-drift test in tests/ng-deploy.bats should verify this (or add assertion) |
| `bats` not installed; `shellcheck` installed | MEDIUM | Install `bats-core` before any script test run; document in CONTRIBUTING.md |
| build.sh bug: wb stub inclusion | MEDIUM | Fix profile detection to check for Java sources, not directory existence; add T4 bats test |
| build.sh bug: single-class bytecode check | MEDIUM | Fix to iterate all `.class` files per jar |
| build.sh: no usage, Spanish messages, stale example path | LOW | Fix in T4 |
| T1 verify-module.sh does not yet exist | HIGH | New file; drives T2; standalone (not ng-deploy.sh API surface) |
| T2 verify-module.bats does not yet exist | HIGH | Needs fixture jars (generated, not real); catches bytecode, SF, module.xml regressions |
| T3 build-n4-kit-links.bats does not yet exist | MEDIUM | Catches dangling ref regressions |

---

## D. Doctrine / Preparation Gaps

### D1. Launcher vs kit primary/fallback inconsistency (F1)

SKILL.md says "Build = `$KIT/toolbelt/build.sh`". BUILD-LOOP.md step 4 says "Primary: ng-deploy.sh --strict-slotomatic. Fallback for quick WSL iteration: toolbelt/build.sh." The kit is correct; the launcher is wrong. Fix: update SKILL.md Hard Rules bullet to match.

### D2. No explicit environment preflight step

A fresh session needs to verify before touching any file:
1. JDK 8 path exists: `ls /usr/lib/jvm/java-8-openjdk-amd64`
2. `niagara_home` candidate identified and pinned plugin version confirmed: `ls <nh>/etc/m2/repository/com/tridium/niagara-module/<version>/`
3. Running station lock: determine if station is live; if yes, create mirror (E1 / B5)
4. Module being built: confirm it is not already locked in the target `modules/` dir

BUILD-LOOP.md step 0 ("Orient") covers corpus/exemplar reading but has no preflight. Add a "preflight" sub-step. build-verify.md should add a "Deploy target & signing" checklist (B1-B7 synthesis).

### D3. Retros have no review-status marker

Kit retros do not carry the `<!-- review-status: pending -->` marker that niagara-research retros carry for `sweep-retros.sh`. After folding, each retro should get `<!-- review-status: folded v0.2 · 2026-09-01 -->` added at line 1. This is the propose-never-apply doctrine's audit trail.

### D4. ng-deploy.sh slotomatic gap (do not fix in this change)

ng-deploy.sh runs slotomatic only for `:MODULE-rt`. A `@NiagaraType` servlet in `-ux` is silently not regenerated. This is a gap in ng-deploy.sh, but fixing it is an API surface change (MINOR bump to niagara-tools). Defer to a separate SDD change; document the gap in build-verify.md so operators know to use build.sh (which runs `:ux:slotomatic`) when editing ux annotations.

### D5. Bitácora 5cuartos retro richer than kit version

`Cliente/Leon-Guanjuato/bitacora/2026-09-01-dashboardpan-5cuartos.md` (239 lines) is richer than the kit's 42-line `2026-09-01-dashboardpan-5rooms.md`. The C-group deltas above are derived from the kit retro; a diff of the bitácora version may surface additional unabsorbed lessons. Flag as an open question for the propose phase.

---

## E. Options Compared

| Option | Description | Pros | Cons | Effort | Fits 800-line budget? |
|---|---|---|---|---|---|
| 1 — Doc fold-in only | Fold all A-D + F1-F3 deltas into existing kit files; fix dangling refs; update retro status markers | Minimal scope; zero new scripts; all 28 items land | Gate gap remains (ng-deploy doesn't verify bytecode/SF); build.sh bugs unfixed; no tests for new content | Low (mostly doc editing ~200-250 lines added) | Yes (single PR) |
| 2 — Fold + toolbelt + tests (RECOMMENDED) | Option 1 + promote mirror-niagara-home.sh (E1) + new verify-module.sh (T1) + fix build.sh (T4) + new tests (T2, T3) + exec bit fix + SKILL.md fix | Closes gate gap; build.sh is now correct; regressions caught by tests; mirror pattern is in the toolbelt | Requires bats install; 3 new files in scripts/tests; ~3 PR slices | Medium (~3 slices, each <400 lines authored) | Yes with auto-chain |
| 3 — Restructure kit type guides | Introduce JACE.md, supervisor.md, signing.md alongside existing types | Cleaner separation of deploy-target concerns | Big restructure; breaks existing links; over-engineering at v0.1 seed stage; logic/wb still SEED | High (~400+ lines restructured) | No (single PR); risky |

**Recommendation: Option 2.** The 28 doc items + 3 toolbelt/test additions fit within the review budget using auto-chain. The gate gap (ng-deploy verify) is a real risk the operator has already experienced (stale jars); verify-module.sh directly addresses it without touching ng-deploy.sh's API surface.

### Suggested PR slices (auto-chain, 800-line budget):

**Slice A — Documentation fold-in** (~250 lines authored):
- `types/dashboard.md`: C1–C13 additions (new subsections: Extending an existing dashboard, chart, plano)
- `types/logic.md`: A1–A4 additions + fix dangling refs
- `types/wb-widgets.md`: fix dangling refs
- `build-verify.md`: B1–B8 additions (new "Deploy target & signing" checklist + B7 STORED repackage recipe)
- `METHODOLOGY.md`: D1–D2 additions + F2 note (preflight sub-step)
- `SOURCES.md`: ColdRoomPan exemplar path update
- Kit retros: add `<!-- review-status: folded v0.2 -->` markers
- `BUILD-LOOP.md`: preflight sub-step in step 0

**Slice B — Toolbelt + build.sh + exec bit + SKILL.md** (~120 lines authored):
- `toolbelt/mirror-niagara-home.sh`: promote from bitácora (copy verbatim, 36 lines)
- `toolbelt/build.sh`: fix wb stub detection, all-classes bytecode check, usage, English messages, stale example
- `toolbelt/verify-module.sh`: new script (T1), 7 automatable checks, ~60 lines
- `scripts/ng-deploy.sh`: restore exec bit
- `~/.claude/skills/build-n4-module/SKILL.md`: fix build primary/fallback wording (F1)

**Slice C — Tests** (~150 lines authored):
- `tests/verify-module.bats`: T2, tests for verify-module.sh with fixture jars
- `tests/build-n4-kit-links.bats`: T3, relative ref resolution check
- `tests/build-sh.bats` additions: T4, stub -wb dir skip test
- `CONTRIBUTING.md`: add bats-core install step (linuxbrew: `brew install bats-core`)
- CHANGELOG.md + VERSION bump (0.3.0→0.4.0, MINOR: new tools + scripts)

---

## F. Constraints

### F1. Propose-never-apply doctrine

The three kit retros are the canonical "propose-never-apply" artifacts. Folding is the human-equivalent act we are explicitly authorized to perform now. After folding:
- Do NOT delete retro files
- Add `<!-- review-status: folded v0.2 · 2026-09-01 -->` as the first line of each retro
- This preserves audit history and satisfies the METHODOLOGY marker convention

### F2. TDD requirement for scripts/

CONTRIBUTING.md §2 is strict: any change to `scripts/*.sh` follows red→green→refactor with bats. This means:
- Slice C (tests) must be written before Slice B is completed on script changes, OR the apply phase writes them in the correct order
- For build.sh (in toolbelt/, not scripts/), the TDD rule technically applies only to `scripts/*.sh`, but the same discipline should apply given it is test-infra-capable
- bats-core must be installed before the gate can run; document in CONTRIBUTING.md

### F3. Real-purpose test requirement

Each test must have a concrete regression it catches:
- `verify-module.bats`: catch jars with forged major byte (65 instead of 52); catch missing NIAGARA4.SF; catch module.xml type not having matching .class
- `build-n4-kit-links.bats`: catch the existing dangling refs in logic.md + wb-widgets.md (and any new ones)
- `build-sh.bats` T4: catch build.sh incorrectly including a stub -wb dir that has no Java sources

### F4. Version bump

Adding verify-module.sh (new optional toolbelt script) and any new env vars = MINOR bump. 0.3.0 → 0.4.0. If Slice B touches ng-deploy.sh only for the exec bit (chmod) with no flag/exit/env surface change, that is a PATCH. However, any CONTRIBUTING.md update + new KB topics = MINOR by convention. Use 0.4.0.

---

## G. Open Questions for Propose Phase

1. **Bitácora 5cuartos diff**: `bitacora/2026-09-01-dashboardpan-5cuartos.md` (239 lines) vs kit's 42-line version — are there unabsorbed lessons not covered by C1–C13? Propose phase should do a targeted diff and determine if additional C-group items exist.

2. **STORED repackage script**: should B7 be documented only as a recipe in `build-verify.md`, or should there also be a `toolbelt/stored-repack.sh`? Adding a script triggers TDD; the recipe-only approach is simpler. Propose phase should decide based on frequency of use (JACE-bound modules only).

3. **verify-module.sh: standalone vs ng-deploy flag**: a standalone `toolbelt/verify-module.sh` (T1) avoids touching ng-deploy.sh's API surface (no VERSION bump beyond MINOR). Alternatively, a `--verify-module` flag in ng-deploy.sh would integrate the gate into the primary workflow but is a MINOR API surface addition. Recommend standalone for this change; flag integration can be a later SDD change.

4. **SOURCES.md ColdRoomPan path**: update to `/home/cristian/modulos_niagara_n4/Cliente/Leon-Guanjuato/Paccadia/ColdRoomPan/` as primary, note `/mnt/c/...` as Windows-only fallback. Propose phase should confirm this is intended (it is the same physical directory under WSL mount vs native).

5. **ng-deploy.sh -ux slotomatic gap** (D4): document the gap in this change or defer? Recommend documenting (in build-verify.md, under the slotomatic section) without fixing, to keep this change's API surface clean.

6. **review-status markers in kit retros**: the kit retros are currently untracked in git. The fold-in step should commit them with their `<!-- review-status: folded v0.2 -->` markers. Propose phase should decide if the retros are committed as part of Slice A or separately.

---

## Affected Areas (files to change in v0.2)

```
build-n4-module-kit/
  METHODOLOGY.md                         — D1, D2, preflight sub-step
  BUILD-LOOP.md                          — preflight step 0
  build-verify.md                        — B1–B8 (deploy target & signing checklist)
  SOURCES.md                             — ColdRoomPan path update
  types/dashboard.md                     — C1–C13 (Extending, chart, plano subsections)
  types/logic.md                         — A1–A4 + fix dangling refs
  types/wb-widgets.md                    — fix dangling refs only
  toolbelt/build.sh                      — T4 fixes
  toolbelt/mirror-niagara-home.sh        — NEW (promote from bitácora)
  toolbelt/verify-module.sh              — NEW (T1)
  retros/2026-08-31-coldroompan-rt-hardening.md   — add folded marker
  retros/2026-09-01-dashboardpan-5rooms.md         — add folded marker
  retros/2026-09-01-dashboardpan-hmi-touch-ux.md  — add folded marker
~/.claude/skills/build-n4-module/
  SKILL.md                               — F1 fix + primary/fallback wording
niagara-tools/
  scripts/ng-deploy.sh                   — exec bit restore only
  tests/verify-module.bats               — NEW (T2)
  tests/build-n4-kit-links.bats          — NEW (T3)
  tests/ng-deploy.bats                   — addenda for T4 (build.sh stub-wb test)
  docs/GOTCHAS.md                        — new entries: STORED repackage, mirror pattern, verify-module
  docs/knowledge-base/verify-module.md  — NEW KB topic (if GOTCHAS entry warrants it)
  CONTRIBUTING.md                        — bats-core install step
  CHANGELOG.md + VERSION                 — 0.3.0 → 0.4.0
  openspec/changes/build-n4-module-kit-v0.2/   — SDD artifacts
```

---

## Ready for Proposal

Yes. The delta inventory is complete, deduplicated, and sourced. The three PR slices fit within the 800-line review budget under auto-chain. The open questions are bounded and do not block proposal-level decisions.
