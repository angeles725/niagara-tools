# C9 retro pins — pre-staged (QA), to fold at close (PR13) into `retros/2026-09-xx-campaign9-*.md`

Each pin: what happened (with the evidence), why it matters, the rule to fold. Status column is filled at
close: `folded` (with the K-id or doc §) or `dropped` (with the reason).

| # | Pin | Evidence (2026-09-06 unless noted) | Rule to fold | Status |
|---|---|---|---|---|
| RP1 | **Stale-checkout reads.** The local client checkout `…/Cliente/Leon-Guanjuato` sits at `4f5f1c7` while origin/main is `a109249`; three kit REDs hardcode that path for their real-tree smokes (`demand-in-scope.bats:27`, `lint-silent-protection.bats:30-31`, `ext-writable-shape.bats:26`), and the S12-B RED's first framing ("guard 4 is THE production gap") came from the same stale tree. The C9 design records its own K21 read-tip failure. | A smoke against a stale tree is evidence about the wrong bytes. | Real-tree smokes take a root override (`C9_CLIENT_ROOT`, like `c9-close.bats:108` already does) and the verify runbook runs the tool against the current origin/main worktree as well; a read that cites a client line carries the SHA it was read at. | |
| RP2 | **Line numbers vs content.** tasks.md anchors mutations on line numbers (`:274-288`, `:33`, `:1287`, `:1994`, `:403-407`); every one of them moves the moment the PR lands. | A line-number anchor is silently wrong after any edit above it. | Named mutations carry a CONTENT anchor (an ERE that must match before mutating); `qa/mutate.sh` refuses to apply when the anchor is absent (`ANCHOR-MISSING`) and refuses a sed that changes nothing (`NO-OP-MUTATION`). | |
| RP3 | **Harness-only declared in the header.** PR8 CRA1/2/3 live, PR9 CPB5, R14 lockout + AuditEvent cannot go green in WSL (Baja `ExceptionInInitializerError` offline). The RED headers carry a COVERAGE MAP that names them; the lead's pin count excluded them. | Otherwise a `skip` gets counted as green, or a WSL-impossible pin blocks a PR forever. | A RED file that covers an adapter states, in its header, which spec pins are harness-only; the close gate requires the harness run record (`CLOSE-harness-run`), a SKIP is never a PASS. | |
| RP4 | **The executable RED wins over prose.** CL6: research block B830 said 400, the RED said 401, the lead confirmed 401 and the design (fb9f0d4) recorded the divergence. tasks.md still says DS2 flips for the field-scan mutation (it is DS3), CPB4 for the reseed drop (executable pin is `CPB_W4`), EW1 for the `@NiagaraAction` exemption (it is EW3), `tests/lint-demand-scope.bats` (file is `tests/demand-in-scope.bats`), `vendorVersion` in `module.xml` (key is `defaultModuleVersion` in the GROUP `build.gradle.kts`). | Two contracts for one behaviour means one of them is wrong and nobody notices until verify. | When prose and an executable RED disagree, the RED is the contract; the prose gets a one-line correction pointing at the RED (never the other way round). Mutation tables name the executable pin ids. | |
| RP5 | **Tool-name convention drift.** S7 shipped as `demand-in-scope.sh` and S19 as `ext-writable-shape.sh`; the kit convention is `lint-*.sh`. Both REDs were re-issued (d0f5942, 28feb42) with the row token unchanged. | A path token in a RED is a contract the apply worker builds against. | New lint REDs are named from the convention on day one: file `toolbelt/lint-<x>.sh`, bats `tests/lint-<x>.bats`, row token `<x>`; the close tool-pins loop keys on the bats basename. | |
| RP6 | **zsh failed-glob aborts the whole command.** `--include=*.java` and `nre*.jar` (no match in cwd) made grep/javac never run; the first Baja probe read as inconclusive because of it. | A silent no-run looks like a zero result. | Quote every glob passed to a tool (`--include='*.java'`) and give jars by explicit path in runbooks; a tool that "found nothing" is checked with `echo $?` on the tool, not on the pipe's tail. | |
| RP7 | **Pipeline exit codes.** `tool \| tail; echo $?` reported tail's 0 (slot-coverage "exit=0" false read); `grep -c` on several files prints `file:count` and broke arithmetic. | Wrong exit codes fake a green. | `set -o pipefail` or `${PIPESTATUS[0]}` in every runbook line that measures a tool; `grep -c` on exactly one file or `grep -h … \| wc -l`. | |
| RP8 | **Empty pushes.** Two branches went up with no diff: a heredoc into a directory that did not exist at the base (tunnel `instalacion/pipeline/test/` at 9acb47c) and a python `re.sub` bad escape that never wrote the file. | A pushed tip with no content is a false "landed". | `git show --stat HEAD` (non-empty, expected paths) before every push; the reply to the lead quotes the file list, not only the SHA. | |
| RP9 | **Presence-only pins let defects through** (carried from C8 PR20: four station-logic rule defects passed a `*"CHECK14"*` pin). | A pin that asserts "a row exists" cannot see a wrong count or subject. | Every smoke pin asserts exact counts AND subjects (`grep -c … -eq N`, subject names present/absent); the C9 smokes (SP-smoke, EW10) are already subject-bound; tighten at close if a count is knowable. | |
| RP10 | **Wrong bases silently drop merged PRs** (C8 PR13, PR16). | A branch cut from a stale main reverts other people's work on ff-merge. | Runbook V1: `git merge-base --is-ancestor origin/main HEAD` on every verify; the lead rebases before the QA ping (BUILD-LOOP §7 order, PR12). | |
| RP11 | **Attribution trailers.** The session tooling injects `Co-Authored-By`/`Claude-Session` trailers; the repo rule is 0 trailers and the close gate asserts it. All C9 QA commits went out trailer-free by explicit default; the policy conflict was surfaced to the owner and stays with them. | The close gate turns a stray trailer into a red close. | Trailer grep is in every runbook (V1) and in `c9-close.bats`; the commit template rule stays "conventional commits only". | |
| RP12 | **Baja offline boundary.** With baja/control-rt/alarm-rt/nre on the classpath a client `BComponent` compiles but cannot be constructed in WSL. | It decides what a WSL RED can honestly assert. | Adapter features split into a pure seam (compile-RED) + source-structural pins (assertion-RED) + harness-only pins declared in the header (RP3); `qa/junit-run.sh` auto-selects Baja-free sources. | |
| RP13 | **Cross-PR couplings need an owner at verify time.** SP-smoke's CR-3 row flips absent when PR8 lands (R3↔R8); MIR5 re-pins when R14 lands; PR9 bumps the version line PR1 set; PR6 and R14 share `Dashboard/build.gradle.kts:33`. | The second PR to merge inherits an obligation nobody re-reads. | The runbook lists the coupling on BOTH PRs with the check to run ("if X already merged, expect Y"); the lead's settle order records which side updated the pin. | |
